// All functions will recieve an express(like) request and response
// and can be called from the app using the path '/api/<functionName>'

export default {
  // exampleFunction: (req, res) => {},
};
